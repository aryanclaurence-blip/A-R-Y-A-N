<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="rootWindow"
        Title="ARYAN — Wall Orientation QA"
        Height="780"
        Width="1040"
        MinHeight="520"
        MinWidth="720"
        WindowStartupLocation="CenterScreen"
        Background="#070B14"
        ResizeMode="CanResizeWithGrip"
        FontFamily="Segoe UI Variable Display, Segoe UI"
        FontSize="13"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <SolidColorBrush x:Key="BrushBackground" Color="#070B14"/>
        <SolidColorBrush x:Key="BrushSurface" Color="#111827"/>
        <SolidColorBrush x:Key="BrushSurfaceAlt" Color="#1A2332"/>
        <SolidColorBrush x:Key="BrushBorder" Color="#2A3548"/>
        <SolidColorBrush x:Key="BrushText" Color="#F4F7FB"/>
        <SolidColorBrush x:Key="BrushTextMuted" Color="#8B9BB4"/>
        <SolidColorBrush x:Key="BrushAccent" Color="#00D4FF"/>
        <SolidColorBrush x:Key="BrushAccentHover" Color="#33E0FF"/>
        <SolidColorBrush x:Key="BrushDanger" Color="#FF4D4F"/>
        <SolidColorBrush x:Key="BrushSuccess" Color="#3DDC97"/>
        <LinearGradientBrush x:Key="BrushHeaderGlow" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#102038" Offset="0"/>
            <GradientStop Color="#0B1220" Offset="0.55"/>
            <GradientStop Color="#101A2E" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="BrushPrimaryButton" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00C8FF" Offset="0"/>
            <GradientStop Color="#0077FF" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="BrushPrimaryButtonHover" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#33D8FF" Offset="0"/>
            <GradientStop Color="#3399FF" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BrushSurface}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="0" Opacity="0.28"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="StatValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,12"/>
            <Setter Property="Margin" Value="0,0,12,12"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{StaticResource BrushPrimaryButton}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BrushPrimaryButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                             To="1.03" Duration="0:0:0.15"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1.03" Duration="0:0:0.15"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                             To="1" Duration="0:0:0.15"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1" Duration="0:0:0.15"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="#243044"
                                BorderBrush="#3A4A63"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2D3C54"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="#4A1F24"
                                BorderBrush="#7A2E36"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#61262E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="rootWindow"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.35"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#070B14" Offset="0"/>
                <GradientStop Color="#0D1424" Offset="0.45"/>
                <GradientStop Color="#08101C" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" MinHeight="180"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="18" Padding="20" Margin="0,0,0,14">
                <Border.Background>
                    <StaticResource ResourceKey="BrushHeaderGlow"/>
                </Border.Background>
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#335EEBFF" Offset="0"/>
                        <GradientStop Color="#112A3548" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <Border.BorderThickness>1</Border.BorderThickness>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" MinWidth="220"/>
                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                        <ColumnDefinition Width="Auto" MinWidth="100"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Width="56" Height="56"
                            CornerRadius="14"
                            Margin="0,0,18,0"
                            VerticalAlignment="Center">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#123B5C" Offset="0"/>
                                <GradientStop Color="#0A1E33" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="A"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#00D4FF"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="txtToolName"
                                   Text="ARYAN"
                                   Foreground="{StaticResource BrushText}"
                                   FontSize="28"
                                   FontWeight="Bold"/>
                        <TextBlock Text="Wall Orientation QA"
                                   Foreground="{StaticResource BrushAccent}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,2,0,0"/>
                        <TextBlock Text="Enterprise validation for visible walls in the active view"
                                   Foreground="{StaticResource BrushTextMuted}"
                                   FontSize="12"
                                   Margin="0,6,0,0"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="16,0" VerticalAlignment="Center">
                        <TextBlock Text="REVIT VERSION" Style="{StaticResource LabelStyle}"/>
                        <TextBlock x:Name="txtRevitVersion" Text="--" Style="{StaticResource ValueStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Margin="16,0" VerticalAlignment="Center">
                        <TextBlock Text="STATUS" Style="{StaticResource LabelStyle}"/>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="statusPulseDot" Width="8" Height="8" Fill="{StaticResource BrushSuccess}" Margin="0,5,8,0"/>
                            <TextBlock x:Name="txtStatus" Text="Ready" Style="{StaticResource ValueStyle}" Foreground="{StaticResource BrushSuccess}"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="4" VerticalAlignment="Center">
                        <TextBlock Text="REFERENCES" Style="{StaticResource LabelStyle}"/>
                        <TextBlock x:Name="txtReferenceCountHeader" Text="0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource BrushText}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Actions -->
            <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
                <DockPanel LastChildFill="True">
                    <TextBlock DockPanel.Dock="Top" Text="Actions" Style="{StaticResource SectionTitleStyle}"/>
                    <UniformGrid Columns="4" Rows="1" HorizontalAlignment="Stretch">
                        <Button x:Name="btnSelectReferences"
                                Content="Select References"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,10,0"/>
                        <Button x:Name="btnOverrideView"
                                Content="Override Current View"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,10,0"/>
                        <Button x:Name="btnExportInvalid"
                                Content="Export Invalid Walls"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,10,0"/>
                        <Button x:Name="btnClearOverrides"
                                Content="Clear Overrides"
                                Style="{StaticResource DangerButtonStyle}"
                                Margin="0"/>
                    </UniformGrid>
                </DockPanel>
            </Border>

            <!-- Statistics -->
            <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="120"/>
                        <ColumnDefinition Width="*" MinWidth="120"/>
                        <ColumnDefinition Width="*" MinWidth="120"/>
                        <ColumnDefinition Width="*" MinWidth="120"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{StaticResource BrushSurfaceAlt}" CornerRadius="14" Padding="16" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="REFERENCE COUNT" Style="{StaticResource LabelStyle}"/>
                            <TextBlock x:Name="txtStatReferenceCount" Text="0" Style="{StaticResource StatValueStyle}" Foreground="{StaticResource BrushText}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="{StaticResource BrushSurfaceAlt}" CornerRadius="14" Padding="16" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="PARALLEL" Style="{StaticResource LabelStyle}"/>
                            <TextBlock x:Name="txtStatParallel" Text="0" Style="{StaticResource StatValueStyle}" Foreground="#3DDC97"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="{StaticResource BrushSurfaceAlt}" CornerRadius="14" Padding="16" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="PERPENDICULAR" Style="{StaticResource LabelStyle}"/>
                            <TextBlock x:Name="txtStatPerpendicular" Text="0" Style="{StaticResource StatValueStyle}" Foreground="#00D4FF"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="3" Background="{StaticResource BrushSurfaceAlt}" CornerRadius="14" Padding="16">
                        <StackPanel>
                            <TextBlock Text="INVALID" Style="{StaticResource LabelStyle}"/>
                            <TextBlock x:Name="txtStatInvalid" Text="0" Style="{StaticResource StatValueStyle}" Foreground="#FF6B6B"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Log -->
            <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,14">
                <DockPanel LastChildFill="True">
                    <TextBlock DockPanel.Dock="Top" Text="Activity Log" Style="{StaticResource SectionTitleStyle}"/>
                    <Border Background="#060A12"
                            BorderBrush="{StaticResource BrushBorder}"
                            BorderThickness="1"
                            CornerRadius="12">
                        <ScrollViewer x:Name="scrollLog"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto">
                            <TextBox x:Name="txtLogOutput"
                                     Background="Transparent"
                                     Foreground="#B8C7DA"
                                     BorderThickness="0"
                                     IsReadOnly="True"
                                     TextWrapping="Wrap"
                                     FontFamily="Cascadia Mono, Consolas, Courier New"
                                     FontSize="12"
                                     Padding="14"
                                     AcceptsReturn="True"/>
                        </ScrollViewer>
                    </Border>
                </DockPanel>
            </Border>

            <!-- Footer -->
            <Border Grid.Row="4"
                    Background="{StaticResource BrushSurface}"
                    BorderBrush="{StaticResource BrushBorder}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="14,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="txtFooterStatus"
                               Text="Ready"
                               Foreground="{StaticResource BrushTextMuted}"
                               VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1"
                               Text="View overrides only  •  No geometry modification  •  Revit 2020–2028"
                               Foreground="{StaticResource BrushTextMuted}"
                               FontSize="11"
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
